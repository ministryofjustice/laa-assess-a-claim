apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-assess-claims.fullname" . }}-test-connection"
  labels:
    {{- include "laa-assess-claims.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-assess-claims.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
